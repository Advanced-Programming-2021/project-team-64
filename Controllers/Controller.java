package Controllers;
public class Controller {
    // here we will have all common functions for controllers
    // like the functions for handling regexs that arrangement of inputs is not important for them
    // or have optinal imputs
    // we will handle them all in controller and use them in other controllers
}